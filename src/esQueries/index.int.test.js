import esConnect from '../esConnect.js'
import { getAllLanguages } from "./index.js"

describe("test index functions", () => {
  it("gets all languages of a dataset", async() => {
    const languages = await getAllLanguages()

    
  
  })

})